package com.growdigitalbridge.performance.domain;

/** DATABASE.md documents only "cycle dates/status" with no enumerated states; this is the minimal set. */
public enum ReviewCycleStatus {
    ACTIVE,
    CLOSED
}
