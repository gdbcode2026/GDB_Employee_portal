package com.growdigitalbridge.performance.domain;

/** Only "submission" is documented (API.md's POST .../submit); no further stages are specified. */
public enum ReviewStatus {
    DRAFT,
    SUBMITTED
}
