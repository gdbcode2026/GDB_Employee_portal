package com.growdigitalbridge.project.domain;

/** DATABASE.md documents a "role" field on ProjectMembership without an enumerated vocabulary; this is the minimal set. */
public enum MembershipRole {
    MEMBER,
    LEAD
}
