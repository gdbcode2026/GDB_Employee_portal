package com.growdigitalbridge.project.domain;

/** REMOVED (not a deleted row) preserves the audit trail DATABASE.md requires for membership changes. */
public enum MembershipStatus {
    ACTIVE,
    REMOVED
}
