package com.growdigitalbridge.attendance.api.dto;

/** The only two outcomes a direct decision endpoint accepts; see WORKFLOWS.md's approve/reject model. */
public enum Decision {
    APPROVED,
    REJECTED
}
